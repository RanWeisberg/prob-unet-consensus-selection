"""Training loop, configuration and diagnostics.

Import submodules directly, e.g. ``from probunet.training.trainer import Trainer``.
Nothing is re-exported here, so ``python -m`` on a submodule stays clean.
"""
